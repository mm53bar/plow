#!/usr/bin/env bash

if [ -n "$start" ]; then
  echo "Processing recipe: start"

  ssh $APP@$SERVER "sh -l -c 'cd $DEPLOY && \
                    bin/bluepill --no-privileged -c $DEPLOY/tmp load ./config/bluepill.rb'"
fi
